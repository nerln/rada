#!/usr/bin/env python3
"""Comando `rada`."""
import os
import sys

ROOT = os.path.dirname(os.path.dirname(os.path.realpath(__file__)))
sys.path.insert(0, ROOT)

from rada.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
